// Imports
import {colors} from './colors';
import utils from './utils';
import fonts from './fonts';

const theme = {
  colors,
  utils,
  fonts,
  buttonHeight: 48,
  inputHeight: 55,
  roundness: 5,
  inputRoundness: 16,
};

export default theme;
